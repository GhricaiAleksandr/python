# =====================================================================
# 💾 АВТОМАТИЧЕСКОЕ СОХРАНЕНИЕ КОДА В ФАЙЛ (БЕЗ ДУБЛИРОВАНИЯ)
# =====================================================================
# Как это работает:
# 1. 'In[1]' — системный массив Colab/Jupyter, в котором хранится
#    ВЕСЬ текст (код, комментарии к строкам, псевдокод) из Ячейки №1.
# 2. open("guess_number.py", "w", encoding="utf-8") — создает файл
#    'guess_number.py' в режиме записи ("w") с кодировкой UTF-8
#    (чтобы русский язык в комментариях не превратился в «крякозябры»).
# 3. f.write(...) — записывает весь содержимый текст из Ячейки №1 в файл.
# 4. Конструкция 'with ... as f:' автоматически закрывает и сохраняет файл
#    после завершения записи.

with open("guess_number.py", "w", encoding="utf-8") as f:
    f.write(In[1])

print("✅ Файл 'guess_number.py' успешно сформирован и готов к отправке!")